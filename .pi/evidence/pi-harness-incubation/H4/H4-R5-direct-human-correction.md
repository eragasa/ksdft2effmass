# Direct human correction after R4

Human instruction:

> you need to change the hard-coded rule like what was asked

The instruction was given immediately after the root PI agent reported that the remaining hard-coded full-suite total is the same validator defect class, that changing it modifies a replay input, and that completing the correction therefore requires a successor replay-input boundary. It authorizes the omitted deterministic correction: apply the same non-hard-coded, same-run pytest status/count rule to the full Python suite. No other correction or scope expansion is authorized.
