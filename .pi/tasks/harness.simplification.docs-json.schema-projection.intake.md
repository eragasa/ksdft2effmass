# Schema-projection Task intake

This page retains human-readable intake and explanatory context only. It is not authoritative, cannot activate work, cannot supply or override scope, status, prerequisites, completion, or acceptance, and is never an input to the generated control-reference page.

The authoritative Task-local record is [`harness.simplification.docs-json.schema-projection.json`](harness.simplification.docs-json.schema-projection.json). The selected chain owns membership, order, active-task relation, explicit activation facts, and automatic-successor policy.

The pilot was requested to establish one file-per-Task JSON boundary with deterministic full-page documentation projection. Future SQLite portability motivates clean identifiers and separated relations only; no database or event-log implementation belongs to this Task.
