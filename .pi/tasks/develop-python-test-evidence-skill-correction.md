# TEST-EVIDENCE-SKILL-1 — develop-python-test-evidence harness correction

Status: implementation and review correction complete; blocked at pending final human checkpoint `TEST-EVIDENCE-SKILL-1-HC01`

## Authority and scope

The current human instruction titled **Harness skill correction — create `develop-python-test-evidence`** authorizes one reusable generic test-evidence skill, separation of test-evidence ownership from `document-python-research-software`, deterministic structural validation, local-route activation, read-only forward validation, one implementation writer, one independent reviewer, one final human checkpoint, and commit/push.

The task may change directly affected generic resources under `harness/pi/`, local profile/extension resources under `harness/local/`, routed live skills and capability records under `.pi/skills/`, directly affected skill-consumer agent records, maintained harness/test-evidence documentation, and task-specific control/evidence records. It must not modify P2 provenance tests, P2 production source, P2 schemas/fixtures, or P2 checkpoint disposition.

## Required boundary

- Canonical skill identity: `develop-python-test-evidence`.
- Generic source is authoritative; local resources depend on generic resources and never the reverse.
- `document-python-research-software` retains public source/API/concept/Sphinx and serialization-contract documentation, but not primary test creation or grammar ownership.
- Exactly `class_owned` and `artifact_owned` remain generic primary ownership kinds.
- Strict structural validation applies only to explicitly supplied migrated/new test paths; legacy debt is inventoried separately.
- Forward validation is read-only against the provenance record class tests and one operator numerical-verification family.
- No scientific calculation, external-tool execution, P2 mutation, publication, release, or successor activation is authorized.

## Completion

The canonical/local/live skill correction, deterministic validator, controlled fixtures, current local replay adapter, forward validation, maintained documentation, and consumer migration are complete. Generic/local resources, seven-skill capability inventory, selected local route, 23 controlled cases, Ruff, Sphinx warnings-as-errors, links, JSON, task completion/ownership/checkpoint gates, dependency/lockfile and P2 nonmutation, and unrelated-work preservation pass.

The single independent reviewer returned three High findings. The sole implementation writer corrected all three in one consolidated pass; parent deterministic validation passes afterward. No second reviewer or repeated replay cycle was launched. Historical H4 evidence is unchanged. The selected `local` route passes; direct legacy use requires separate restoration of historical resource bytes.

Final human acceptance is pending at `.pi/checkpoints/TEST-EVIDENCE-SKILL-1-HC01-final-acceptance.json`. No P2 test cleanup or successor is active.
