# Reconcile evidence-ID auditing and retire duplicate AST policy

Status: completed under `.pi/chains/harness-simplification.chain.json`

Task identity: `harness-simplification.evidence.audit-action-conformance`

Starting revision: `c04436ff60f3b1817dcf034f907e3195602655db` (`origin/dev` after fetch with a clean worktree).

Authority: the current human instruction authorized one bounded root-agent production parser correction, thin maintained CLI, focused maintained evidence, live-caller migration, duplicate-script retirement after explicit gates, current profile/inventory synchronization, documentation, and minimum chain closeout. It prohibited delegation, review, maintained-test migration or ID renumbering, historical-evidence rewrites, agent changes absent a live route, dependencies, checkpoints, scientific execution, telemetry, and successor activation.

## Correction and deterministic owner

`AuditEvidenceIdentifiers.execute` retains its public signature and stateless explicit-input behavior. The parser now uses normalized docstring indentation and one cohesive private declaration parser for current multiline `Evidence ID` fields, retained historical first lines, and inclusive same-prefix ranges. It rejects multiple or empty fields, independent IDs, invalid ranges, malformed IDs, profile namespace/scope violations, marker defects, duplicate owners, invalid source, and exact protected gaps with existing closed issue codes. Occurrence lines remain one-based function-definition lines and result ordering remains deterministic.

The project-local module `ksdft2effmass.harness.pi.local.audit_evidence_identifiers` is the only maintained CLI projection. It requires an explicit absolute root, profile, and inventory; verifies exact inventoried module SHA-256 identities; reads only those modules beneath the root; invokes the public ActionObject; emits deterministic JSON; and writes nothing. `AuditEvidenceIdentifiers` remains the sole parser and policy owner. `ValidatePythonTestEvidence` separately owns complete module structural convention, while `develop-python-test-evidence` owns semantic design and review.

## Maintained repository result

The exact current inventory contains 201 modules and 1,102 top-level executable test functions. The maintained CLI reports PASS with 1,102 occurrences, 1,102 unique evidence IDs, zero issues, and an empty issue-code count. Current profile rules were synchronized to already-maintained `SV-HARNESS` and `SV-TEV` owners; no existing evidence ID was changed or renumbered.

## Retirement gates

Focused action grammar tests and CLI artifact tests pass, including API agreement, explicit-root confinement, exact inventory, nonrepository CWD, no discovery, and nonmutation. The current inventory passes. All live command routing and maintained documentation use the new CLI, no current agent or skill routes to the old script, the capability inventory names `AuditEvidenceIdentifiers` as sole deterministic owner, and neither resource manifest identifies the retired file. Therefore `.pi/skills/audit_evidence_identifiers.py` is deleted. Historical evidence, checksums, reports, logs, and prior task records remain unchanged and may continue to name the command actually used.

## Focused validation

- Action and CLI grammar/artifact suites: 24 passed; the focused public-import module brings the combined focused pytest result to 26 passed.
- Maintained structural test-evidence validation: PASS for the two changed modules, 12 top-level test functions, 12 unique owners, and no findings.
- Maintained-inventory CLI audit: PASS for 201 modules, 1,102 top-level test functions/occurrences, 1,102 unique IDs, and no issues.
- Ruff format and lint: PASS for four changed Python files.
- Focused mypy: PASS for four changed Python files.
- Public imports, capability inventory, local resource-manifest/profile compatibility, selected profile identity stability, modified relative links, Sphinx warnings-as-errors, and `git diff --check`: PASS.

## Closeout

The chain `active_task` is `null`. Slice 7 retirement of `inspect-task-state` remains deferred. Telemetry, historical routing reconciliation, and all other successors remain inactive and unauthorized. Structural PASS makes no semantic, numerical, scientific-validation, UQ, provenance, or human-acceptance claim.
