# Intake: simplify the durable HarnessTask architecture

The human selected Option C at the pending Stage-2A acceptance boundary and
authorized one bounded correction. Retain the version-2 `HarnessTask` core,
serializer, deserializer, graph validator, mixed-format `TaskRecordAdapter`, and
`TaskStateInspector` compatibility. Remove the unaccepted migration-framework
interfaces, commands, skill routing, projection profile, packet machinery, and
focused tests that exist only for those surfaces. Preserve all six authoritative
Markdown Task files byte-for-byte, keep Stage 2B inactive, and do not migrate a
Task or activate a successor.
