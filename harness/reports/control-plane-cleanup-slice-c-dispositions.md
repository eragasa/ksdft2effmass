# Control-plane cleanup Slice C correction dispositions

## Exhaustive scope

The single permitted correction pass covers 613 deduplicated surfaces: all 322 original unresolved surfaces, all 44 proposed Slice C candidates, and all 247 Slice B retentions. Final dispositions are **503 retain**, **110 delete**, and **0 unresolved**. The JSON report supplies every human-required field for every entry.

## Operational decisions

Twenty-one obsolete phase agents and their generated inventory page are deleted. Newly orphaned resolved/superseded checkpoints and historical execution/replay evidence are deleted when no current Task, chain, public contract, declarative resolver, or scientific/research authority consumes them. Historical links and tests alone do not retain a surface. Current Task-referenced intakes, accepted-contract evidence, scientific/research-referenced decisions, manifest/profile-selected schemas and fixtures, maintained public modules/exports, skills, durable agents, current Sphinx pages, and tests following retained behavior remain.

All 13 CLIs are retained with a distinct operation recorded under `cli_analysis`. All retained completed Tasks have exact current chain, Task-relationship, or accepted-authority relationships under `retained_completed_task_justifications`.

## Validation and state

Affected deterministic gates pass. The complete configured suite passes all 2,985 tests after provisioning the already declared build-system requirements (`pip`, `setuptools>=77`, and `wheel`) into the ignored local environment from local/offline sources; neither `pyproject.toml` nor `uv.lock` changed. The single consolidated review identified five deterministic findings, all resolved in the permitted bounded correction pass. Slice C is complete, the Task is awaiting human acceptance, automatic successor activation remains disabled, and no successor is active.

## Architecture candidates (advisory only)

Later work may evaluate compiler-style repository loading with normalized state, public validator composition, explicit projector ownership, synchronizer/comparator separation, restricted Pi action interfaces, typed action requests, and capability-constrained operator profiles. These are non-authoritative candidates only; Slice C implements none of them and creates no Task, checkpoint, API, or runtime surface for them.

## Human acceptance and closeout

Accepted claim:

> The bounded control-plane cleanup removed repository surfaces proven obsolete under the current architecture, eliminated unresolved dispositions, preserved operational behavior, and retained complete validation agreement.

This acceptance does **not** claim that the repository now has a minimal control plane. The accepted residual architectural limitations are:

- 112 Task records and 112 generated Task pages remain.
- 85 completed Tasks remain operationally referenced.
- 77 archive files remain.
- 13 CLIs remain.
- 87 harness Python modules remain.
- 316 of the original 322 unresolved surfaces were retained.
- Current authority, historical relationships, runtime validation, and generated projections remain structurally coupled.
- Further reduction requires a later architectural change rather than another cleanup correction.

The previously recorded architecture candidates remain non-authoritative proposals and were not activated, implemented, or elaborated during closeout. No successor is active. The next separately authorized project activity should be a simulation-first scientific vertical slice: `tutorial input → smoke-test execution → raw artifact retention → manual inspection → minimal extraction → deterministic replay`.
