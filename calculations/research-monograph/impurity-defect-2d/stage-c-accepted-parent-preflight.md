# Accepted-parent Stage C design preflight

## Authority

- [x] HC11 records the verbatim response `recommendation authorized`.
- [x] The normalized decision is `AUTHORIZE_ACCEPTED_PARENT_STAGE_C_DESIGN_ONLY`.
- [x] HC13 records the verbatim response `adverserial implementation plan,
  implementation, and adverserial review authoried` and normalizes it to
  `AUTHORIZE_EXECUTION_FREE_ACCEPTED_PARENT_STAGE_C_IMPLEMENTATION`.
- [x] HC13 authorizes authored-fixture implementation and review only.
- [x] HC15 records `recommendation authorized` for execution-free accepted-
  artifact adapter, authorization/provenance, verifier, test, and review work.
- [x] HC16 records `recommendation authorized` for the human instruction
  `storage for all simulations should be in ~/projects/ksdft2effmass` and binds
  `/Users/eugene/projects/ksdft2effmass` without authorizing population.
- [x] Parent review performed an unauthorized pre-journal `sha256sum` byte read
  over the five accepted inputs. It performed no parsing, semantic inspection,
  calculation, output creation, external-root access/mutation, or new information
  gain; all digests matched the already-frozen identities.
- [x] First human response `recommendation authorized` disposes that parent
  action as a retained procedural deviation and authorizes HC17 preparation only;
  it does not consume or authorize HC17.
- [x] During that preparation, the worker mistakenly ran aggregate `SHA256SUMS`,
  byte-reading only `stage-a-result.json`, `stage-b-result.json`, and
  `stage-c-design.json` before attempt consumption. It performed no semantic
  parsing, scientific computation, executable authorization, attempt consumption,
  accepted-result creation, or external-root access; all three checks matched
  frozen identities. The command also redirected its ordinary `OK` lines to the
  transient `/tmp/stage-c-hc17-checksums.log`, not a repository or Stage C result.
- [x] Second human response `recommendation authorized` disposes the worker action
  as a second retained procedural deviation. At that preparation boundary HC17
  remained unresolved and unconsumed.
- [x] Before the authorized attempt, no semantic accepted-parent read by Stage C
  code and no Stage C execution had occurred. The later HC17 Workflow consumed
  STARTED before its accepted-input identity checks and semantic reads.
- [x] Stage B reruns, Stages D--E, material work, publication, and release remain
  unauthorized.

## Immutable inputs

- [x] Pre-attempt aggregate checksum verification is prohibited whenever the
  catalog contains any accepted input.
- [x] Every preparation check must use an explicit safe-file allowlist excluding
  all five accepted inputs. Their identities must not be byte-read, re-hashed, or
  parsed again before attempt consumption.
- [x] The protected Workflow may validate accepted-input identities only after
  exclusive STARTED creation consumes the sole attempt; its package checksum
  catalog covers only new Stage C products.
- [x] Accepted periodic input SHA-256 is
  `82e9915101e755f7cc77cb8901478f88d878ffaa808732036631b7ae5cfd78ec`.
- [x] Accepted periodic result SHA-256 is
  `4eb55bde9d456d86bad1d65c8ac60267c07873c6936f8af876b07fd3e1d27be6`.
- [x] Stage A result SHA-256 is
  `e6f34cc8a4246b46b963dfef193b07d259427822fa8f280acc5ae4068998d283`.
- [x] Stage B result SHA-256 is
  `711c91064e498a183fab9a0ad48f115779bdcb6059e0c23d84270aac75867d9b`.
- [x] Human-accepted execution-free Stage C design SHA-256 is
  `609c8dd88b27c0ee45870ad67ae525ff2b50832b3fc2d4d8dd770241871a5f15`.
- [x] Paths are canonical repository-relative paths beneath the repository root.
- [x] The parent deviation re-observed matching digests, but this adds no new
  evidence claim and does not replace the previously recorded identities.

## Represented meaning

- [x] Dimension, ordering, units, energy zero, spin convention, geometry, and
  twists are explicit.
- [x] Isotropic, anisotropic, and axis-swapped parents are separately identified.
- [x] The anisotropic hopping preprocessing and truncation residual are separate
  from defect-model error.
- [x] $D_4$ covariance is limited to the isotropic parent.
- [x] The anisotropic parent uses $D_2$ plus an explicit separate-parent axis swap.
- [x] Directional and diagonal nonlocal plants include Hermitian reverse bonds.
- [x] Every model-class basis vector, real fit coefficients, residual operand
  order, class order, and no-selection stop are explicit.
- [x] Model selection cannot see the planted expected class before selection.

## Route and schedule controls

- [x] Route A and Route B construct matrices independently from compact inputs.
- [x] The gauge bridge is an equivalence diagnostic rather than a third vote.
- [x] The known attack, inverse map, source phase, translation, and energy shift
  are frozen.
- [x] A-then-B and B-then-A run in separate fresh spawned processes and each
  independently reconstructs the anisotropic hopping inventory.
- [x] Serialization order is independent of execution order.
- [x] Route or schedule disagreement is retained without selection or averaging.

## Inventory and evidence

- [x] The exact inventory is 208 route evaluations, 104 bridges, 1,040 model
  fits, and 104 cross-schedule route comparisons.
- [x] Every model class and failed criterion remains retained.
- [x] Locality shells, core-exterior coupling, and support identity are explicit.
- [x] Ten adverse controls distinguish alignment, energy, model, Hermiticity,
  gauge, twist, symmetry, axis-swap, and route-independence failures.
- [x] The independent verifier is forbidden to import or consume runner matrices
  and uses orthogonal projections or QR rather than normal equations.
- [x] An append-only attempt journal, closed compact result, independent log,
  deterministic plot, report, manifest, and package checksum catalog are
  required for any later execution.

## Resource and next-boundary gates

- [x] The proposed envelope is dimension 64, two schedules, 600 seconds, 2 GiB,
  20 MiB output, no network, no external executable, and no new dependency.
- [x] The authored implementation behavior completed in 1.56 seconds with
  99,041,280 bytes maximum resident set size and a 2,205,921-byte result.
- [x] The authored behavior satisfies the 600-second, 2-GiB, and 20-MiB envelope.
- [x] Both anisotropic adverse controls produce $6.0\times10^{-3}E_G$, above
  their predeclared $10^{-3}E_G$ floors.
- [x] Maintained authored modes either reject accepted-parent status or exercise
  the five-record adapter exclusively with embedded synthetic records.
- [x] Dormant future execution mode validates exact checkpoint, repository root
  `/Users/eugene/worktrees/ksdft2effmass-calculations`, Git revision
  `9def2718ee763faf2060eb692739600485de5c72`, machine `minerva`, external native
  root, ordered operation and artifact identities, seven frozen compact output
  paths, declared resource bounds, one-attempt/no-retry policy, and absent
  outputs before semantic accepted-input reads.
- [x] Exclusive attempt-journal creation occurs before accepted-input content
  hashing; STARTED or terminal FAILURE consumes the attempt and blocks retry.
- [x] Every retained output uses exclusive creation, and the complete authored
  operation produces and checksum-verifies all seven artifacts.
- [x] The independent verifier imports no runner, uses independent inverse
  Fourier reconstruction and QR, reconstructs all 208 routes and 1,040 fits,
  and independently binds accepted-mode authority and provenance.
- [x] The closed result and execution-authorization schemas, accepted-capable
  deterministic SVG, exclusive-write refusal, and 28 maintained software-
  verification tests are implemented.
- [x] Runtime and peak memory are measured and checked only after computation;
  600 seconds and 2 GiB were not proactive hard sandbox limits. HC17 preserved
  this limitation.
- [x] HC12 human-adopts the exact proposed design; HC13 authorizes execution-free
  implementation but neither checkpoint authorizes execution.
- [x] HC14 human-accepts the exact execution-free implementation boundary and
  authorizes managed administrative closeout only.
- [x] Compact future records remain under repository `calculations/**`; native
  simulation work is separately bound to the existing external root
  `/Users/eugene/projects/ksdft2effmass`.
- [x] HC17 resolved with preserved response `yes`, normalized decision
  `AUTHORIZE_ONE_ACCEPTED_PARENT_STAGE_C_EXECUTION`, and clarification
  `WRONG WINDOW, HC17`.
- [x] The one authorized attempt is consumed: the append-only journal records
  STARTED then terminal SUCCESS with no error and no retry.
- [x] The immutable seven-artifact package exists. All 17 declared criteria pass;
  the independent verifier reports PASS with maximum scalar difference
  `2.741586977772928e-15 E_G`.
- [x] Observed runtime is 2.729825292015448 seconds, peak resident memory is
  97,894,400 bytes, and retained package size is 2,223,699 bytes, within the
  post-computation 600-second, 2-GiB, and 20-MiB bounds.
- [x] Parent read-only structural review reports `NO_BLOCKING_FINDINGS` without
  rerunning the scientific verifier.
- [x] HC18 preserves human response `1`, accepts the exact immutable HC17
  calculated numerical-verification package, and authorizes its managed
  administrative closeout on `work/calculations` with upstream
  `origin/work/calculations`.
- [x] Acceptance does not establish scientific or material validation and does
  not complete the Task.
- [ ] Task completion, Stage D or E, rerun, and successor activation remain
  unauthorized.
