# Execution-free accepted-parent Stage C adapter implementation plan

## Authority and stop boundary

HC15 records the verbatim human response `recommendation authorized` and permits
only execution-free adapter, authorization/provenance, schema, independent-
verifier, maintained-test, review, and bounded durable-state work. HC16 records
`recommendation authorized` for the separate human storage instruction
`storage for all simulations should be in ~/projects/ksdft2effmass`, normalized
to the exact external root `/Users/eugene/projects/ksdft2effmass`. Future
authority is also frozen to repository root
`/Users/eugene/worktrees/ksdft2effmass-calculations`, Git revision
`9def2718ee763faf2060eb692739600485de5c72`, and machine `minerva`. All behavior
must use authored compact fixtures and framework scratch outputs. The new path
must not read the accepted periodic, Stage A, or Stage B artifacts, populate the
external root, create an accepted-parent result, invoke verification or plotting
on accepted evidence, or activate Stage D. A later exact protected-execution
decision is reserved for HC17 or the next nonconflicting identity after this
implementation is frozen.

## Sole-writer path ownership

One writer owns only these bounded paths for this operation:

- `.pi/checkpoints/research-monograph-impurity-defect-2d-stage-c-accepted-parent-adapter-implementation.json` and `.pi/checkpoints/research-monograph-impurity-defect-2d-stage-c-native-storage-root.json`;
- `calculations/research-monograph/impurity-defect-2d/{run_stage_c_parent.py,verify_stage_c_parent.py,stage-c-result.schema.json,stage-c-execution-authorization.schema.json}`;
- `calculations/research-monograph/impurity-defect-2d/stage-c-accepted-parent-{adapter-implementation-plan,adapter-implementation-review,protocol,preflight}.md`;
- bounded synchronization in `calculations/research-monograph/impurity-defect-2d/{README.md,protocol.md,preflight.md,SHA256SUMS}`;
- `python/tests/software_verification/ksdft2effmass/research_monograph/impurity_defect_2d/test__stage_c_execution_contract.py` and its existing ownership record;
- `python/tests/software_verification/ksdft2effmass/research_monograph/impurity_defect_2d/resources/{stage-c-accepted-parent-adapter-authored-fixture.json,stage-c-execution-authorization-authored-fixture.json}`;
- `tasks/research/research-monograph.exercises.impurity.defect-2d.json`; and
- deterministic Harness projections only if the canonical synchronizer requires them.

No source, test, documentation, task, or calculation path outside that list is
owned. No concurrent writer or ownership manifest is required for this sole-
writer operation.

## Object and wire ownership

- an immutable execution-authorization DataObject owns only its decoded fields;
- an execution-authorization deserializer owns the closed JSON wire mechanics;
- an accepted-parent artifact adapter owns conversion of five closed source
  records into the compact isotropic hopping and anisotropic-energy fixture used
  by the already accepted finite study;
- an authority validator owns canonical paths, hashes, checkpoint decision,
  exact input identities, output paths, repository/machine identity, and resource
  declarations before any accepted artifact read;
- an append-only attempt journal atomically consumes the attempt before the first
  accepted-parent content hash or read and retains terminal success/failure;
- one protected Workflow owns the ordered runner, independent verifier, plotter,
  report, native-manifest, checksum-finalization, and attempt-finalization steps;
- the serializer and retained-artifact writers own extended provenance and
  exclusive creation, refusing replacement or oversized output;
- the independent verifier parses the retained provenance and authored adapter
  fixture without importing runner code; and
- `main()` remains only the typed `argparse` adapter for mutually exclusive
  authored-fixture, authored-adapter, and future accepted-execution modes.

## Implementation sequence

1. Add a closed Stage C execution-authorization schema and exact typed decoding.
2. Validate canonical repository root and Git revision, machine, the exact external native artifact
   root `/Users/eugene/projects/ksdft2effmass`, checkpoint, code/schema/input
   hashes, compact repository output paths, resource ceilings, and the no-network/
   no-external-executable policy before opening any accepted input.
3. Add one accepted-artifact adapter from periodic input/result, Stage A result,
   Stage B result, and execution-free Stage C contract to the frozen compact
   `ParentFixture`; exercise the same conversion with one authored compact
   multi-record fixture only.
4. Extend the result schema and serializer with exact input, authority, machine,
   repository, resource, runtime, peak-memory, output-byte, operation-inventory,
   attempt, and retained-output identities while preserving authored labels.
5. Add independent verifier reconstruction of adapter-authored results, with no
   runner import, accepted input, normal equations, or runner matrix/cache use.
6. Add one typed complete-operation Workflow and exclusive producers for result,
   verification log, accepted-capable SVG, report, native evidence manifest, and
   checksum catalog; finalize the append-only attempt journal last.
7. Add cohesive artifact-owned software-verification cases for pre-read failure,
   adapter conversion, provenance/schema closure, deterministic bytes, exclusive
   output creation, complete-package production, failure retention, no retry,
   resource/output bounds, and independent verification.
8. Run cheapest format/lint/type/compile/tests first, then Python evidence,
   schema, checksum, Harness, documentation, and deterministic reproduction
   checks applicable to the changed paths.
9. Perform an adversarial implementation review, freeze final source/schema/
   fixture/output-path identities, report any blocker, and stop before HC17.

## Frozen future authority and retained paths

The later execution question, if separately authorized, must use checkpoint
`RM-IMPURITY-DEFECT-2D-STAGE-C-ACCEPTED-PARENT-EXECUTION-HC17` at
`.pi/checkpoints/research-monograph-impurity-defect-2d-stage-c-accepted-parent-execution.json`
and authorization identity
`research-monograph.impurity-defect-2d.stage-c.accepted-parent-execution.hc17.v1`
at `calculations/research-monograph/impurity-defect-2d/stage-c-accepted-parent-execution-authorization.json`.
Neither file exists in this implementation boundary.

That later authority must bind these repository-relative non-existing compact
targets:

- `calculations/research-monograph/impurity-defect-2d/stage-c-accepted-parent-attempt.jsonl`;
- `calculations/research-monograph/impurity-defect-2d/stage-c-accepted-parent-result.json`;
- `calculations/research-monograph/impurity-defect-2d/stage-c-accepted-parent-verification.log`;
- `calculations/research-monograph/impurity-defect-2d/stage-c-accepted-parent-summary.svg`;
- `calculations/research-monograph/impurity-defect-2d/stage-c-accepted-parent-report.md`; and
- `calculations/research-monograph/impurity-defect-2d/stage-c-accepted-parent-native-evidence-manifest.json`; and
- `calculations/research-monograph/impurity-defect-2d/stage-c-accepted-parent-SHA256SUMS`.

Every artifact is exclusively created. The attempt journal is created before
accepted-input hashing, remains after all failures, and prevents rerun even when
later outputs are absent. A numerical or operational FAIL is retained without
selection, averaging, tolerance changes, replacement, or automatic retry.
Runtime and peak-memory values are post-computation observations and rejection
checks, not proactive operating-system sandbox limits; HC17 must state this
limitation exactly unless hard enforcement is separately implemented.
